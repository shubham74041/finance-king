import React from 'react';

const LogoutPage = () => {
    return (
        <div>
            <h2>Logout Page</h2>
            {/* Add content specific to logout page */}
        </div>
    );
};

export default LogoutPage;
