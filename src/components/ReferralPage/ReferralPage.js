import React from 'react';

const ReferralPage = () => {
    return (
        <div>
            <h2>Referral Page</h2>
            {/* Add content specific to referral */}
        </div>
    );
};

export default ReferralPage;
